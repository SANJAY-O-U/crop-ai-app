# ── LOCAL DEVELOPMENT ────────────────────────────────────────────────────────
# Leave VITE_API_URL empty so the Vite proxy (vite.config.js) handles /api
# This avoids CORS entirely on localhost.
VITE_API_URL = https://your-app.onrender.com
